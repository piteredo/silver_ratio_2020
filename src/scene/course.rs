pub mod map;
pub mod ball;
pub mod aside;
// pub mod counter(include pre / main?)

#[derive(Debug)]
pub struct Course {
    // map: Map,
    // ball: Ball,
    // counter: Counter, (include pre / main?)
    // wait_time: (include pre wait, after wait?)
    // aside: Aside;
    // ratio: Vec<u32, u32>,
    turn_length: u32,
}
